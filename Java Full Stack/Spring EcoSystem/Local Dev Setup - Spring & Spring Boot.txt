Technology Stack:
Java, Spring MVC, Spring Boot, Embedded Tomcat, RESTful APIs (using Spring Boot), Maven, Git
PostgreSQL (Database Server/DBMS), pgAdmin 4 (Client Tool), Spring Data JPA (Hibernate)
HTML, CSS, JSP, JavaScript, Bootstrap 

=======================================================================================================================================================================
Setup Java (JDK):

Install JDK 17 (Recommended for Spring Boot 3+)

Download:
https://www.oracle.com/java/technologies/downloads/#java17-windows

After installation, verify:
java -version
javac -version
You should see version 17

Add Environment Variable:
JAVA_HOME = C:\Program Files\Java\jdk-17

Add to path:
%JAVA_HOME%\bin

Verify:
echo %JAVA_HOME%

=======================================================================================================================================================================
Setup Maven (Optional, if you want to use embedded Maven in the IDE):

Download:
https://maven.apache.org/download.cgi 

Setup
Extract zip → C:\Program Files\Apache\maven

Add Environment Variable:
MAVEN_HOME = C:\Program Files\Apache\maven

Add to Path: 
%MAVEN_HOME%\bin

Verify:
mvn -version
You should see Java version + Maven version.

=======================================================================================================================================================================
Setup Git:

Download:
https://git-scm.com/ 

Verify:
git --version

Create account in GitHub and configure  user name and user email:
git config --global user.name "Your Name"
git config --global user.email "your@email.com"

Verify:
git config user.name
git config user.email
=======================================================================================================================================================================
Setup IDE:

Spring Tools for Eclipse:

Download:
https://spring.io/tools#eclipse

After installing configure Java and Maven:

Add Installed JRE/JDK in STS:
Window → Preferences → Java → Installed JREs
Select JDK 17 manually.

Then: 
Window → Preferences → Java → Compiler → Set compliance to 17

If not configured:
Project build errors “Execution environment not supported”


Configure Maven:
Window → Preferences → Maven → Installations

You will see:
Embedded Maven (came with STS)

OR Add External Maven
Select system Maven if installed

Add Eclipse EE and web developer tools:
To be able to work with web projects without spring,
STS does not have Web developer tools by default to work with Dynamic Web Project
Also includes html, jsp, xml, css editors.
Help -> Eclipse Marketplace - look for Eclipse Enterprise Java and Web Developer Tools

=======================================================================================================================================================================
Tomcat Setup:

If Using Spring Boot:
No need to install Tomcat separately
Spring Boot has embedded Tomcat

Run project:
mvn spring-boot:run
OR run main class in IDE.
Then the app runs at:
http://localhost:8080


If Using Traditional Spring MVC (WAR file):
Then you must install Tomcat manually:

Download Tomcat:
https://tomcat.apache.org/

Use:
Tomcat 10 (for Spring 6.x, Spring Boot 3.x and above)
Tomcat 9 (for Spring 5.x, Spring Boot 2.x and earlier)

Extract → D:\Softwares

Add Server Runtime Environment in STS:
Window > Preferences > Server > Runtime Environments --- Add installed tomcat

Add installed Java17 to Tomcat:
Window > Preferences > Server > Runtime Environments --- Edit and add installed Java

Create new server instance in STS:
This step will create a pointer to actual physical tomcat installation folder in your machine.
Window > Show View > Other > Server > Servers --- Open (This will open the Servers Pane view)
In the Servers Pane right click > New > Server > Apache > Select tomcat version (9) > Select the Server Runtime Environment > Finish
If creating multiple servers in the IDE from a single physical tomcat on the machine, make sure to maintain unique port numbers for admin, http, ajp

Change Config Location:
Right click on the server > properties > Switch location > Apply and Save
Double click on the server > Server Locations > User Tomcat installation --- Save

Start the server:
Without IDE:
Go to bin\ inside Tomcat installation path and run:
startup.bat

Or

From Services:
Modify ports in server.xml (8005: shutdown, 8080: HTTP Connector, 8009: The AJP Connector port if used, 8443: SSL/HTTPS Connector port if used), modify SERVICE_NAME, DISPLAYNAME, CATALINA_BASE in service.bat (if you want to have multiple tomcats running simultaneously, optional)
Open CMD
Go to bin\ inside Tomcat installation path and run the command:
service.bat install
Open Services -> Apache Tomcat xx Tomcatx -> Start

Or

From IDE:
Right Click on the server and click Start

Check in the browser:
http://localhost:8080

After configuring Tomcat in STS, make sure that the 'Target runtime' is properly set when you are creating a new project

Deploy: Copy your .war file into:
webapps/
Restart tomcat

=======================================================================================================================================================================
Create Spring Boot Project (Using Maven):

Spring Initializr (Easy Way): 
https://start.spring.io/ 

Select
Project: Maven
Language: Java
Spring Boot: 3.x
Java: 17

Dependencies:
Spring Web
Spring Data JPA
MySQL Driver (if using DB)
Lombok (optional)
Spring Security (if needed)

Group: com.example
Artifact: demo
Name: demo
Description: Demo project for Spring Boot
Package name: com.example.demo
Packaging: Jar
Configuration: Properties

Download → Extract → Open in IDE

=======================================================================================================================================================================
Project Structure:
=======================================================================================================================================================================

In Spring core (non boot), we need to write manual configuration for the dependencies like MVC, Tomcat, JPA/Hibernate etc



Spring Core - Non Web App (console/desktop/stand alone) - JAR (no web server like tomcat)

How to create: Create a standard Maven project. Add spring-context. In your main method, manually bootstrap the Spring environment using AnnotationConfigApplicationContext.

spring-console-jar/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/spring/
│         │          ├── config/
│         │          │    └── AppConfig.java    <-- @Configuration, @ComponentScan
│         │          ├── service/
│         │          └── MainApplication.java   <-- Manually loads AppConfig.class
│         └── resources/
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- project config
└── .gitignore




Spring Core - Server-Side Rendering (html/css/jsp/thymeleaf) - WAR (external tomcat)

How to create: Add spring-webmvc. Implement WebApplicationInitializer to code-configure your DispatcherServlet. JSPs require the webapp/WEB-INF structure. Thymeleaf can reside either in webapp or resources.

spring-ssr-war/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/spring/
│         │          ├── config/
│         │          │    ├── WebConfig.java         <-- ViewResolver configuration
│         │          │    └── WebAppInitializer.java <-- Replaces web.xml
│         │          └── controller/
│         ├── resources/
│         └── webapp/
│              └── WEB-INF/
│                   └── views/
│                        └── index.jsp                <-- Target for InternalResourceViewResolver
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- packaging: war
└── .gitignore




Spring Core - Server-Side Rendering (thymeleaf/Freemarker/mustache) - JAR (embedded tomcat)

How to create: Set packaging to jar. Add spring-webmvc and tomcat-embed-core. Write code in your main method to instantiate a new org.apache.catalina.startup.Tomcat instance, add your Spring context, and bind it to a port. Templates are loaded directly from your classpath resources/.

spring-ssr-embedded-jar/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/spring/
│         │          ├── config/
│         │          │    └── WebConfig.java    <-- Configures ThymeleafViewResolver
│         │          ├── controller/
│         │          └── StandaloneApp.java     <-- Contains custom manual Tomcat initialization
│         └── resources/
│              └── templates/                   <-- Thymeleaf templates processed from classpath
│                   └── home.html
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- packaging: jar
└── .gitignore




Spring - REST APIs - WAR (external tomcat)

How to create: Set packaging to war. Configure DispatcherServlet programmatically. Return serialisable domain objects marked with @RestController or @ResponseBody.

spring-rest-war/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/spring/
│         │          ├── config/
│         │          │    ├── WebConfig.java
│         │          │    └── WebAppInitializer.java
│         │          └── user/
│         │               ├── controller/       <-- @RestController Endpoints
│         │               └── model/
│         ├── resources/
│         └── webapp/
│              └── WEB-INF/                     <-- Empty or minimal, needed for valid WAR construction
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- packaging: war
└── .gitignore




Spring Core — REST APIs — JAR (Embedded Tomcat)

How to create: Same execution as project #7, but without the template files or HTML rendering configurations. The programmatically launched embedded Tomcat handles incoming requests and serialises Java POJOs directly to JSON.

spring-rest-embedded-jar/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/spring/
│         │          ├── config/
│         │          ├── product/
│         │          │    ├── controller/
│         │          │    └── model/
│         │          └── EmbeddedTomcatLauncher.java <-- Setup code for Tomcat instance
│         └── resources/
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- packaging: jar
└── .gitignore



///////////////////////////////////////////////////////////////////

Spring boot removed all the configuration logic for the dependencies and added optional auto-configuration feature



Spring Boot - Non Web App (console/desktop/stand alone) - JAR (no web server like tomcat)

How to create: Use Spring Initializr (start.spring.io), select standard Java, and do not add the "Spring Web" starter. Implement CommandLineRunner to execute code immediately after startup.

springboot-console-jar/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/boot/
│         │          ├── service/
│         │          └── BootConsoleApplication.java <-- Implements CommandLineRunner
│         └── resources/
│              └── application.properties       <-- spring.main.web-application-type=none
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- packaging: jar
└── .gitignore




Spring Boot - Server-Side Rendering (html/css/jsp/thymeleaf) - WAR (external tomcat)

How to create: Add spring-boot-starter-web. Change packaging to war. Ensure your main class extends SpringBootServletInitializer. JSPs must go into the src/main/webapp folder because they cannot be compiled from inside a Spring Boot JAR classpath.

springboot-ssr-war/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/boot/
│         │          ├── ServletInitializer.java <-- Overrides configure() method
│         │          ├── BootApplication.java    <-- Main method
│         │          └── controller/
│         ├── resources/
│         │    └── application.properties       <-- spring.mvc.view.prefix=/WEB-INF/jsp/
│         └── webapp/
│              └── WEB-INF/
│                   └── jsp/
│                        └── welcome.jsp
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- packaging: war
└── .gitignore




Spring Boot - Server-Side Rendering (thymeleaf/Freemarker/mustache) - JAR (embedded tomcat)

How to create: Add spring-boot-starter-web and spring-boot-starter-thymeleaf. Keep packaging as jar. Spring Boot auto-configures Tomcat and hooks it directly into the templates/ folder structure.

springboot-ssr-embedded-jar/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/boot/
│         │          ├── controller/            <-- Returns template string names
│         │          └── BootSsrApplication.java
│         └── resources/
│              ├── static/                      <-- CSS, JavaScript, Images
│              ├── templates/                   <-- HTML view layout (Thymeleaf)
│              │    └── profile.html
│              └── application.properties
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- packaging: jar
└── .gitignore




Spring Boot - REST APIs - WAR (external tomcat)

How to create: Add spring-boot-starter-web, set packaging to war, configure SpringBootServletInitializer. This allows you to hand over routing responsibilities to an IT-managed standalone app server cluster.

springboot-rest-war/
│
├── src/
│    └── main/
│         ├── java/
│         │    └── com/example/boot/
│         │          ├── ServletInitializer.java
│         │          ├── BootRestApplication.java
│         │          └── api/
│         │               ├── controller/       <-- Returns raw JSON data
│         │               └── dto/
│         ├── resources/
│         └── webapp/                           <-- Empty, no views, required placeholder only for maven-war-plugin
│
├── target/     		<-- compiled files (auto-generated)
├── pom.xml            <-- packaging: war
└── .gitignore





Spring Boot - REST APIs - JAR (embedded tomcat)

How to create: The modern baseline configuration for Cloud/Microservice design. Add spring-boot-starter-web, compile directly into an executable flat JAR file, and spin it up using standard container virtualization like Docker.

springboot-rest-embedded-jar/
│
├── src/
│    ├── main/
│    │    ├── java/
│    │    │     └── com/example/demo/
│    │    │           ├── config/
│    │    │           │	└── AppConfig.java
│    │    │           ├── exception/
│    │    │           │	└── GlobalExceptionHandler.java
│    │    │           ├── common/
│    │    │           │	└──	 ResponseUtil.java
│    │    │           ├── user/
│    │    │           │	├── controller/
│    │    │           │	├── service/
│    │    │           │	├── repository/
│    │    │           │	└── model/
│    │    │           ├── order/				  <-- Same Structure
│    │    │           ├── payment/				  <-- Same Structure
│    │    │           ├── ....
│    │    │           └── DemoApplication.java  <-- main class
│    │    │
│    │    └── resources/
│    │          ├── application.properties
│    │          ├── static/
│    │          ├── templates/   <-- Thymeleaf
│    │          └── application.yml (optional)
│    │
│    └── test/
│         └── java/
│               └── com/example/demo/
│
├── target/          <-- compiled files (auto-generated)
├── pom.xml          <-- project config
├── .gitignore
└── mvnw / mvnw.cmd  <-- Maven wrapper
		  

=======================================================================================================================================================================
Database Setup:

PostgreSQL

Download PostgreSQL:
https://www.postgresql.org/download/windows/

Install:
Select Components: Ensure the following are checked:
PostgreSQL Server: The actual database.
pgAdmin 4: The graphical interface you'll use to manage it.
Command Line Tools: Includes psql for terminal access.
Note: You can uncheck "Stack Builder" to keep the installation simple.
Set Password for the superuser (postgres): Do not forget this password, as you need it to log in.
Port & Locale: Keep the default port 5432 and the default locale unless you have a specific reason to change them.
Finish: Click through the remaining "Next" prompts and wait for the installation to complete.


In a spring boot application, for Spring Data JPA:
Add below properties to application.properties file

# PostgreSQL connection settings
spring.datasource.url=jdbc:postgresql://localhost:5432/db_name
spring.datasource.username=postgres
spring.datasource.password=your_postgres_password
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate / JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

=======================================================================================================================================================================
Install Postman:

Download Postman: 
https://www.postman.com/downloads/

=======================================================================================================================================================================
Git Workflow for Projects:

Inside project:
git init
git add .
git commit -m "Initial commit"

Create repo in GitHub → then:
git remote add origin https://github.com/username/project.git
git push -u origin main

